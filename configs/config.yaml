defaults:
  - data: default
  - model: rf
  - _self_

#Output configs
output:
  model_path: "outputs/models/model.pkl"
  metrics_path: "outputs/models/metrics.json"
  log_dir: "logs/${now:%Y-%m-%d}/${now:%H-%M-%S}"
  log_path: "${output.log_dir}/main.log"

# Experiment
experiment:
  name: "test_run_v1"
  track: true

hydra:
  run:
    dir: ${output.log_dir}
